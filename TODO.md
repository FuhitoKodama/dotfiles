# dotfiles 改善 TODO

このファイルは、dotfiles の継続改善アイテムを管理するための TODO 一覧です。

## TODO 一覧（表形式）

| 状態 | 優先度 | アイテム                           | 背景                                     | 方針案                                                                                                                                                                                                                                                                                | 完了条件                                                                  |
| ---- | ------ | ---------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| [x]  | High   | VSCode 拡張機能の自動反映          | 新規環境で毎回手動インストールが必要     | Dev Container: `.devcontainer/devcontainer.json` の `customizations.vscode.extensions` で自動導入<br>ローカル macOS / Linux / Coder: `setup/init/init_vscode_extensions.sh` が `code --install-extension` 系 CLI を実行<br>拡張機能ID一覧は `.dotconfig/vscode/extensions.txt` で管理 | `make all-in-one` 後に主要拡張が揃う<br>`README.md` に更新手順がある      |
| [x]  | High   | MCP 設定の自動反映                 | MCP サーバー設定を手動で入れる必要がある | [docs/mcp.md](docs/mcp.md) で「ナレッジ」と「対話的分析」2パターン分離の基準を定義<br>初期採用: AWS Knowledge MCP と Terraform MCP（ナレッジ+対話分析）<br>CLI 優先（GitHub、Playwright、draw.io 等）はドキュメント記載済み<br>CLI 認証設定は [docs/cli-setup.md](docs/cli-setup.md) で手順化<br>`.dotconfig/vscode/mcp.json` で管理し `deploy_vscode.sh` で反映；秘密情報は input variables で扱う | AWS/Terraform MCP が新規環境で起動直後に利用可能<br>CLI 環境設定（認証）も同時にセットアップ完了<br>単発確認は CLI、複合分析は MCP と役割分離が明確<br>秘密情報をリポジトリに含めない |
| [x]  | High   | セットアップ検証コマンドの標準化   | セットアップ完了判定が属人的             | make doctor を追加<br>brew/ghq/peco/node/git などを一括チェック<br>失敗時に修正ガイドを表示                                                                                                                                                                                           | 1コマンドで状態確認可能                                                   |
| [x]  | Medium | peco から fzf へ完全移行 | peco は開発停止状態で、fzf の方が広く導入されている  | `Brewfile_dev_cli` の peco を fzf に差し替え<br>`bindkey.zsh` の 4 関数 (`peco-history-selection` / `peco-cdr` / `peco-ghq` / `peco-ghq-vscode`) を fzf 版にリネーム + 実装置換<br>`Keybind.md` / `doctor.sh` の peco 記述を fzf に置換<br>履歴選択は fzf が独自ソートするため `--no-sort` を付けて並び順を維持 | `make init-brew` で fzf が入り、`make doctor` が通る<br>Ctrl+R / Ctrl+U / Ctrl+G Ctrl+G / Ctrl+G Ctrl+F が新しい shell で動作 |
| [ ]  | Medium | setup スクリプトの冪等性テスト     | 複数回実行時の安全性を担保したい         | make init-all を2回実行して差分ゼロを検証<br>失敗しやすい箇所に set -eu と fail-fast を徹底                                                                                                                                                                                           | 再実行で壊れない                                                          |
| [ ]  | Medium | Linux/macOS の差分管理を明文化     | 仕様差が README と実装に分散             | docs/compatibility.md を作成<br>対象: Brewfile, VSCode設定反映, init scripts                                                                                                                                                                                                          | OS差分の判断が README だけで可能                                          |
| [ ]  | Low    | install対象のグルーピング見直し    | init-all の粒度調整がしづらい            | make init-core / init-dev / init-ui を追加                                                                                                                                                                                                                                            | 用途別に最小セットを実行可能                                              |
| [ ]  | Low    | 変更履歴の運用開始                 | 何を改善したか追いにくい                 | CHANGELOG.md を導入<br>セットアップ影響のある変更は必ず記載                                                                                                                                                                                                                           | リリース単位で差分把握が可能                                              |
| [x]  | High   | シェルスクリプトの `set -eu` 統一 | `install.sh` と一部 init スクリプトのみ設定済みだった。未定義変数参照・途中エラーの取りこぼしを防ぎたい | 全 `.sh` ヘッダに `set -eu` を追加<br>`source` 対象の `util_deploy.sh` は呼び出し元の設定を継承する旨をコメント化<br>`deploy_vscode.sh` の `$CODESPACES` / `$CODER` は `${VAR:-}` 形式に変更<br>pipefail は POSIX sh 非対応のため別途判断 | 全 `.sh` が `set -eu` 配下で実行される<br>`make deploy-all init-all doctor` が全てパス |
| [ ]  | Medium | `reloadzsh` による PATH 重複の抑止 | `.dotconfig/zsh/rc/alias.zsh:40` の `reloadzsh` 実行で `.zshenv` 内の `PATH` 追記が積み上がる。現環境でも PATH に go/linuxbrew/volta が二重に出ている | `alias reloadzsh='exec zsh -l'` に置換<br>または `typeset -U path PATH` を `.zshenv` 冒頭で宣言して重複除去 | `reloadzsh` を 3 回実行後に `echo $PATH \| tr : '\n' \| sort \| uniq -d` が空 |
| [ ]  | Medium | CI による shellcheck / shfmt 導入 | 現状 `.github/` が無く静的検査が皆無。品質が手動レビュー依存 | `.github/workflows/lint.yml` で `shellcheck setup/**/*.sh install.sh` を実行<br>`Brewfile_dev_cli` に `shellcheck` / `shfmt` を追加して手元でも同じ検査を走らせる | PR で shellcheck がブロッキングとして動作 |
| [ ]  | Low    | `.zprofile` の brew 無条件 eval 除去 | `.zprofile:7` で `eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"` が条件分岐なし。macOS や brew 未導入環境で無駄な実行。`.zshenv` 側に正しい分岐があり二重 | 無条件行を削除し `.zshenv` のガード付き分岐に一本化 | `.zprofile` の brew 行が条件ガード付きのみ |
| [ ]  | Low    | `util_deploy.sh` の source 汚染解消 | `deploy_vscode.sh` が `util_deploy.sh` を `source` しており、`target`/`files` 等の変数が呼び出し元に漏れる。`deploy-all` の連続実行で後続へ影響する可能性 | サブシェル `( . util_deploy.sh ... )` で囲む or 関数化して引数経由に変更 | 実行後に呼び出し元シェルへ変数が残らない |
| [x]  | Medium | ghq での常用リポジトリ一括クローン | 新規環境セットアップ時に常用 repo を毎回手動で `ghq get` する手間がある。ただし GitHub 認証が前提なので `all-in-one` への組込みは不可 | `.dotconfig/ghq/repositories.txt` に `<host>/<owner>/<repo>` 形式で常用 repo 一覧を管理（初期サンプルはコメントのみ）<br>`setup/manual/init_ghq_clone.sh` が一覧を読んで `ghq get -u` を順次実行（既存は update、失敗は集計して続行）<br>`make ghq-clone` として Makefile に追加、`init-all` からは除外<br>README の「GitHub 初期セットアップ」節に手順を追記 | `make ghq-clone` が動き、エラー行はサマリ表示され非失敗行で中断しない<br>`make all-in-one` では実行されない |
| [ ]  | Low    | `init_ghq.sh` の ghq.root 取得を include 対応に | `setup/init/init_ghq.sh:12` が `git config --global --get ghq.root` を使っており、`GIT_CONFIG_GLOBAL` 導入後は include 経由の値を拾えず `GHQ_ROOT` 未設定時に root 作成がスキップされる | `--global` を外して `git config --get ghq.root` に変更（`.zshenv` と同じ対応） | `GHQ_ROOT` 未設定でも `~/.gitconfig` 側の `ghq.root` で root ディレクトリが作成される |
| [x]  | High   | macOS 新規セットアップの Xcode CLT 前提を README / init_homebrew.sh に明記 | 新規 macOS で `make all-in-one` を走らせると Homebrew インストーラが暗黙に Xcode Command Line Tools を要求して失敗するが、README 冒頭に前提条件の記載なし | README の「セットアップ」章直下に「前提条件 (macOS のみ)」を追加し `xcode-select --install` を明記<br>`setup/init/init_homebrew.sh` で `uname -s` が Darwin の場合のみ `xcode-select -p` を実行し、未導入時は案内して exit 1 | Xcode CLT 未導入の macOS で `make all-in-one` を実行すると案内メッセージが出る<br>README 冒頭だけで新規 macOS セットアップの順序が追える |
| [x]  | High   | `deploy_home.sh` の symlink 対象除外不足 | `setup/deploy/deploy_home.sh` がブラックリスト方式で `.??*` を一括 symlink しており、`.devcontainer` / `.claude` / `.gitconfig.local.example` 等まで `$HOME` に展開されていた | ブラックリストをやめ、ホワイトリスト (`.zshenv` / `.zprofile` / `.zshrc` / `.gitconfig` / `.git-templates` / `.dotconfig`) を明示列挙する方式に変更<br>既存環境に残る古い symlink (特に `~/.claude -> repo/.claude`) は Claude Code セッション終了後に手動でクリーンアップする | `make deploy-home` で上記 6 件のみが対象となり、`.devcontainer` / `*.example` の symlink が新規に作られない |
| [x]  | High   | `raws` alias の bash/zsh 非互換と deprecated 依存解消 | 旧実装 `alias raws='unset $(export \| grep AWS \| egrep -o "^[^=]+")'` は `egrep` deprecated で、実際には zsh で空展開時に `unset: not enough arguments` エラーが出ていた（bash は許容、zsh はエラーという逆の想定） | `env \| grep -E '^AWS_' \| cut -d= -f1` で変数名を抽出する関数に書き換え、`[ -n "$vars" ] && unset ...` で空ガード | bash / zsh 両方で AWS_* あり・なしの 4 パターンとも正常動作することを検証済み |
| [ ]  | Medium | README のプロキシ節と `.zlocal` 作成手順の順序整理 | README 冒頭 `### プロキシ設定` は「`.zlocal` に proxy を追加してから」と指示するが、`.zlocal` 作成方法 (`cp ~/.zlocal.example ~/.zlocal`) は後方の「ローカル設定」節に書かれており、上から読むと作成方法が不明なまま `make all-in-one` に進む | プロキシ節に `.zlocal` 作成ワンライナーを併記する or 「事前準備」ブロックを「一括実行」の直前に配置して cp / 編集 / make の順序を明示 | README を上から順に読むだけでプロキシありの新規環境セットアップが完走できる |
| [ ]  | Medium | `Brewfile_dev_cli` の Linux 非互換パッケージガード | `.dotconfig/homebrew/Brewfile_dev_cli:18` の `brew "newrelic-cli"` に OS ガードがなく、Dev Container (Linuxbrew) で無駄なビルド時間が発生。`docker` cask は既に `if OS.mac?` ガード済みで整合性も取れていない | `if OS.mac?` ガードで囲む or `Brewfile_dev_cli_macos` に分離<br>Linux でも使うパッケージが今後増える前提で OS 判定方針を Brewfile に明文化 | Dev Container での `make init-brew` で `newrelic-cli` の install が skip される |
| [ ]  | Medium | `vdot` alias がディレクトリを `vi` に渡している | `.dotconfig/zsh/rc/alias.zsh:70` の `alias vdot='vi $HOME/.dotconfig'` は `.dotconfig` がディレクトリのため、純粋な `vi`/`vim` では `E502: is not a file` でエラー。元々 nvim 前提だった可能性 | 意図に応じて `alias vdot='cd $HOME/.dotconfig'`（移動） or `alias vdot='$EDITOR $HOME/.dotconfig'`（nvim / code で開く想定） or 廃止 | `vdot` 実行で `.dotconfig` を開ける or 移動できる |
| [ ]  | Medium | doctor に `core.hooksPath` 実在チェックを追加 | `.gitconfig:4` の `hooksPath = ~/.git-templates/git-secrets/hooks` が指すディレクトリ不在でも git は黙って hook をスキップする。pre-push ガードが無効化されていても doctor が検知しない | `setup/doctor.sh` の `[Git Configuration]` セクションに `check_directory "git hooksPath" "$HOME/.git-templates/git-secrets/hooks"` を追加 | hooksPath が実在しない環境で `make doctor` が `✗` を出す |
| [ ]  | Low    | `install.sh` で `GIT_CONFIG_GLOBAL` を防御的に export | `install.sh` は Codespaces / Coder の dotfiles feature 経由で `sh install.sh` 相当で実行され、`.zshenv` が source されないため `GIT_CONFIG_GLOBAL` が未設定。現状は影響する箇所が `init_ghq.sh` の 1 箇所のみだが、今後 install フェーズで `git config --global` を呼ぶ追加があると `~/.gitconfig`（symlink）が汚染される | `install.sh` 冒頭に `export GIT_CONFIG_GLOBAL="${GIT_CONFIG_GLOBAL:-$HOME/.gitconfig.local}"` を追加 | `sh install.sh` 実行コンテキストでも `echo $GIT_CONFIG_GLOBAL` が `~/.gitconfig.local` を返す |

## メモ

- 追加したいTODOは、背景・方針案・完了条件の3点をセットで書く
- 優先度は High / Medium / Low の3段階
